# Streamlit Cloud runtime dependencies.
# Keep this file focused on dashboards/streamlit_app.py so Streamlit Cloud can
# resolve wheels on its current Python runtime without installing the optional
# ETL, ML, NLP, Spark, and warehouse toolchain.
pandas>=2.2.3
numpy>=2.1.3
pyarrow>=18.0.0
plotly>=5.24.0
streamlit>=1.39.0
